<!DOCTYPE html>
<html lang="pt-br">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title><%= typeof title !== 'undefined' ? title + ' - ' : '' %>Controle de Estoque</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="/css/styles.css" rel="stylesheet">
</head>
<body>
  <nav class="navbar navbar-expand-lg navbar-dark bg-primary">
    <div class="container-fluid">
      <a class="navbar-brand" href="/estoque">Estoque Hospital</a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav me-auto">
          <li class="nav-item"><a class="nav-link" href="/estoque">Itens</a></li>
          <li class="nav-item"><a class="nav-link" href="/estoque/movimentos">Movimentos</a></li>
          <% if (session && session.user && session.user.perfil === 'ADMIN') { %>
            <li class="nav-item"><a class="nav-link" href="/cadastros">Cadastros</a></li>
          <% } %>
        </ul>
        <ul class="navbar-nav">
          <% if (session && session.user) { %>
            <li class="nav-item"><span class="navbar-text me-2">Olá, <%= session.user.nome %></span></li>
            <li class="nav-item">
              <form method="post" action="/logout">
                <button class="btn btn-outline-light btn-sm">Sair</button>
              </form>
            </li>
          <% } else { %>
            <li class="nav-item"><a class="nav-link" href="/login">Entrar</a></li>
          <% } %>
        </ul>
      </div>
    </div>
  </nav>

  <main class="container py-4">
    <%- body %>
  </main>

  <footer class="text-center text-muted py-3">
    Hugo Leonardo soluções para sistemas
  </footer>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>


